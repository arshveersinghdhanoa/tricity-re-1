name: CI — Build & Test

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

permissions:
  contents: read

env:
  NODE_VERSION: "24"
  PNPM_VERSION: "9.15.0"

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build packages
        run: |
          pnpm --filter @tricity/core build
          pnpm --filter @tricity/pipeline build

      - name: Run tests
        run: |
          pnpm --filter @tricity/core test
          pnpm --filter @tricity/pipeline test
          pnpm --filter @tricity/web test

      - name: Build web app
        env:
          NEXT_PUBLIC_SUPABASE_URL: "https://placeholder.supabase.co"
          NEXT_PUBLIC_SUPABASE_ANON_KEY: "placeholder"
        run: pnpm --filter @tricity/web build

      - name: Verify sitemap and robots
        run: |
          test -f apps/web/.next/server/app/sitemap.xml.meta && echo "sitemap.xml generated" || echo "sitemap.xml missing"
          test -f apps/web/.next/server/app/robots.txt.meta && echo "robots.txt generated" || echo "robots.txt missing"
